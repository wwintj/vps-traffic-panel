# Web 服務配置
HOST=127.0.0.1
PORT=8088

# 面板安全認證 (部署時務必修改為強密碼)
# 允許的字元：大小寫字母、數字、@ _ - . ! %
AUTH_USERNAME=admin
AUTH_PASSWORD=your_secure_password

# 監控網卡 (留空則自動偵測預設網卡)
INTERFACE=

# SSL 憑證配置 (安裝腳本偵測到 Let's Encrypt 憑證並啟用 HTTPS 時自動寫入)
SSL_ENABLED=0
SSL_CERTFILE=
SSL_KEYFILE=
